# obsidian-wiki super setup — Hermes Bootstrap

This file points Hermes to the canonical bootstrap doc.

→ See `AGENTS.md` for the full agent instructions, skill list, and conventions.

→ See `CLAUDE.md` for the vault contract.

→ See `skills/<name>/SKILL.md` for each skill body.

## Skills discovery

Run `bash bin/setup-multi-agent.sh` once to symlink `skills/` into `~/.hermes/skills/obsidian-wiki`. Then Hermes auto-discovers all 14 wiki skills + 6 utilities.

## Critical conventions

See `AGENTS.md` § "Critical conventions" — the 10-rule contract that governs every operation in this vault.
